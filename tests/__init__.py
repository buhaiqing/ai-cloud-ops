"""Test suite — three-layer pyramid per design.md T8.

Markers:
- unit (default) — pure logic, no I/O
- integration — needs DB / Redis / Aliyun SDK mocked
- e2e — full stack, requires running infra
- eval — AI prompt evaluation, requires ANTHROPIC_API_KEY
"""