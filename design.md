# Design: AI-Native Alibaba Cloud Multi-Account Ops Console

Generated by /office-hours on 2026-08-04
Branch: main
Repo: buhaiqing/ai-cloud-ops (placeholder, not yet created)
Status: APPROVED
Mode: Builder
Approved: 2026-08-04 by user via /office-hours

## Problem Statement

作为运维/DevOps 工程师，我管理多个阿里云账号（业务、测试、个人各一），每个账号又跨多个区域（华东 1、华北 2、华南 1、新加坡等）。手动切换账号/区域找资源、配置告警规则、巡检问题效率极低。

已有的工具如阿里云 CloudMonitor 和 ARMS 解决了"看"的问题，但缺少**统一的跨账号/跨区域管理面**，更没有 AI 加持的"诊断 + 建议 + 修复"能力。

目标：开源一个面向运维工程师的 **AI 增强型多账号管理控制台**，让告警从"阈值触发"升级为"AI 诊断"，让告警规则和资源配置可被 AI 编排。

## What Makes This Cool

这不是又一个 Grafana 或 CloudMonitor。差异点：

1. **AI 深度参与是核心范式，不是装饰**——告警触发后，AI Agent 主动调用阿里云 OpenAPI 拉上下文（关联资源、最近变更、同区域同账号的其他指标），输出"根因 + 修复建议 + 可执行命令"三件套
2. **多账号聚合是基础能力**（CloudMonitor 自身做不到跨账号聚合视图，需要 RAM 跨账号授权），统一凭证管理 + 统一规则引擎
3. **告警规则可被 AI 提议**——基于历史告警和资源标签，AI 推荐"还应该配置哪些规则"，从"被动配置"升级为"主动优化"
4. **可扩展的 AI Agent 架构**——后续可加入自动修复（如重启 ECS、扩容 RDS），但 MVP 严格 human-in-the-loop

## Constraints

- **阿里云单云专注**（不做 AWS/GCP 跨云）——差异化 + 复用阿里云特有能力
- **学习/开源目的**——不追求商业产品完整度，但要有 demo 价值
- **多账号凭证安全是头号风险**——必须在文档和架构里明确 AccessKey 管理（推荐 RAM Role + STS 临时凭证）
- **AI 误判成本高**——阿里云操作多数不可逆（如删资源、改配置），所以 MVP 必须有人工审核环节
- **单人开发 + 6-12 个月可持续投入**

## Premises

P1. 痛点真实——多账号管理混乱是当下解不开的痛，不是假设
P2. 阿里云生态有空白——CloudMonitor + ARMS 在 AI 集成、根因诊断方面不够强
P3. 单云专注（只做阿里云）是合理起步
P4. AI 增强 > 传统规则——AI 提供的"根因诊断 + 修复建议"价值大于传统阈值告警
P5. 学习/开源足够驱动持续投入

## Approaches Considered

### Approach A: 最小可行版（CLI + AI 即时分析）

- Summary: Python CLI 调用阿里云 SDK 拉取告警 + 资源信息，喂给 Claude/GPT 出诊断报告，stdout 输出；一账号一区域
- Effort: S（1-2 周）
- Risk: Low
- Pros:
  - 2 周能跑起来，立刻验证"AI 分析告警"是否有真实价值
  - 复用现有阿里云 SDK + LLM API，零基础设施
  - 失败成本极低，写完发现没价值就扔了
- Cons:
  - 一次性分析，不是"持续监控"——偏离你的实际痛点
  - 没有持久化、没有 UI、不像产品
  - 学习价值有限（主要是 prompt 调优）
- Reuses: `aliyun-openapi-python-sdk`、Claude API / OpenAI API

### Approach B: 理想架构（多账号聚合 + AI Agent + Web Dashboard）— 推荐

- Summary: 后台 worker 持续聚合多账号多区域告警入库 → AI Agent 服务做根因诊断 + 修复建议 → Web Dashboard 展示报告并支持 human-in-the-loop 审核；告警规则可视化配置
- Effort: L（3-6 个月，1 人单干）/ 1-2 个月如果削范围
- Risk: Med
- Pros:
  - 完整产品形态——可以截图分享、可以写技术博客讲解架构
  - 学习价值最高：后端 + 前端 + Agent 设计 + 多账号凭证管理 + 实时数据
  - 解决了你说的"管理混乱"——所有账号/区域/资源在一个地方
- Cons:
  - 工作量大，单干 3-6 个月；持久战
  - 多账号凭证管理（AccessKey）是安全雷区——需要在文档里大篇幅讲安全模型
  - 需要持续运行（服务器/部署），OSS 用户不一定愿意跑复杂服务
- Reuses: FastAPI、Next.js、LangGraph 或自研 Agent、PostgreSQL、阿里云 RAM / OpenAPI

### Approach C: 创造性路径（MCP Server + 极简后台）

- Summary: 把阿里云资源/告警包装成 MCP Server，让用户用 Claude Desktop / Cursor / Cline 等 AI 客户端直接对话查询 + 操作；后台只做轻量数据采集（写本地 JSON/SQLite）和告警触发
- Effort: M（4-8 周）
- Risk: Med
- Pros:
  - 站在 MCP 生态风口（2025 年最热的 AI 协议）——OSS 容易火
  - 不造 UI，复用用户已有的 AI 客户端——开发量减半
  - AI 是真正"一等公民"，不是装饰——非常符合"AI 深度参与"方向
- Cons:
  - 依赖 MCP 生态成熟度（用户必须会用 Claude Desktop / Cursor）
  - 持续监控能力弱（更偏"对话查询 + 告警时通知"）
  - 凭证管理问题同样存在，但需要按 MCP 协议设计权限模型
- Reuses: MCP Protocol（Anthropic 标准）、`aliyun-openapi-python-sdk`、Claude API

## Recommended Approach

**Approach B：理想架构（多账号聚合 + AI Agent + Web Dashboard）**

理由：

1. 用户选了"AI 深度参与"——B 的 AI Agent 后端能真正体现这个深度；A 偏单次分析、C 偏交互查询
2. 用户选了"开源/学习"——B 学习价值最高（后端 + 前端 + Agent + 安全模型），且最像"产品"——别人愿意 star
3. B 不必一次做完——可以按 MVP 切片：先做"多账号聚合 + CLI 输出 AI 报告"（就是 A 的多账号版），再叠 Dashboard，再叠自动执行

### MVP 切片（推荐开发顺序）

**Milestone 1（2 周）—— 多账号聚合 + CLI 版 AI 分析**

- 多账号凭证管理（RAM Role + STS Token 方案）
- 持续 worker 拉取告警（CloudMonitor API）
- CLI 输出 AI 诊断报告
- 单账号单区域 MVP，验证 AI 分析是否有真实价值

**Milestone 2（4 周）—— Web Dashboard MVP**

- Next.js 最小 UI：账号/区域/资源列表 + 告警时间线
- 告警详情页：AI 报告展示
- 告警规则 CRUD（最简版）

**Milestone 3（4 周）—— AI Agent 增强**

- Agent 拉取上下文（关联资源、最近变更）
- 修复建议从"通用文本"升级为"结构化操作清单"
- 人审核 + 一键执行（dry-run 优先）

**Milestone 4（持续）—— 高级功能**

- 告警规则 AI 推荐
- 自动修复（受限操作清单 + 完整审计）
- 多区域 + 国际账号支持
- Webhook 集成（Slack / 飞书 / 钉钉）

### 架构草图（Milestone 1-2）

```
┌────────────────────────────────────────────────────────┐
│  Web Dashboard (Next.js)                               │
│  - 账号/区域/资源列表                                   │
│  - 告警时间线 + 详情                                    │
│  - 告警规则 CRUD                                        │
└────────────────────────────────────────────────────────┘
                          ▲
                          │ REST API
                          ▼
┌────────────────────────────────────────────────────────┐
│  Backend (FastAPI)                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │ Ingest      │  │ AI Agent    │  │ Rules       │    │
│  │ (CloudMon)  │  │ (诊断引擎)  │  │ Engine      │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│         │                │                  │          │
│         └────────┬───────┴──────────────────┘          │
│                  ▼                                      │
│         ┌─────────────────┐                            │
│         │  PostgreSQL     │                            │
│         │  - alerts       │                            │
│         │  - resources    │                            │
│         │  - analyses     │                            │
│         └─────────────────┘                            │
└────────────────────────────────────────────────────────┘
                          ▲
                          │ aliyun SDK
                          ▼
┌────────────────────────────────────────────────────────┐
│  Aliyun Accounts (multi, multi-region)                 │
│  - CloudMonitor (告警)                                 │
│  - ECS / RDS / SLB (资源)                             │
│  - ActionTrail (审计)                                  │
└────────────────────────────────────────────────────────┘
```

### 关键技术决策

1. **凭证管理**：推荐 RAM Role + STS 临时凭证（避免硬编码 AccessKey）；配置文件存 RoleARN，运行时换 STS Token
2. **AI Agent 选型**：起步用 Claude API + 结构化 prompt，不引入 LangGraph（避免过度工程）；后续如果 Agent 复杂度提升再引入
3. **数据存储**：PostgreSQL（资源、告警、分析结果）；Redis（任务队列）
4. **部署形态**：Docker Compose 单机起步，便于 OSS 用户本地跑；后续上 Kubernetes

## Open Questions

1. **AI 模型选型**：Claude / GPT / Qwen / DeepSeek？阿里云用户可能倾向 Qwen / DeepSeek（合规 / 成本），但 Claude 在结构化诊断上更强。MVP 建议先支持多个，按账号可配置。
2. **告警规则 AI 推荐**：什么时候做？需要积累多少数据才能推荐？这块不在 MVP 内。
3. **多租户**：是否要做"团队协作"（多个运维共享一个 dashboard）？OSS 起步建议单人单实例，团队协作作为可选扩展。
4. **国际账号支持**：阿里云有 Alibaba Cloud 国际版（新加坡等），是否做？国内 OSS 用户为主，建议先做国内，国际作为后续。
5. **告警规则是配置还是代码**：走 UI 配置还是 IaC（Terraform / Pulumi）？建议 MVP 走 UI 配置简单版，后续加 IaC 导出。

## Success Criteria

- [ ] MVP（Milestone 1）2 周内跑通：1 个阿里云账号 1 个区域，告警能拉到 AI 诊断输出到 CLI
- [ ] 6 个月内（Milestone 1-2）：Web Dashboard 上线，3 个账号 5 个区域跑通
- [ ] GitHub 上 100+ star（验证 OSS 价值）
- [ ] 至少 5 个外部用户试用并给反馈
- [ ] 至少 1 篇技术博客讲解架构（扩大影响力 + 复盘）

## Distribution Plan

- **代码仓库**：GitHub（公开 OSS）
- **分发形态**：
  - Docker Compose（一键启动：Postgres + Backend + Web）
  - Helm Chart（K8s 部署，作为后续）
- **CI/CD**：GitHub Actions
  - Lint + Test（Python: ruff + pytest；TS: eslint + vitest）
  - Docker 镜像构建 + 推送到 GitHub Container Registry (ghcr.io)
  - 每个 tag 自动发布 release
- **文档站点**：MkDocs + GitHub Pages
  - Quickstart（10 分钟跑起来）
  - 架构文档
  - 安全模型（凭证管理重点讲）

## Next Steps

按 MVP 切片顺序推进。第一周的具体任务：

1. **Day 1-2：项目脚手架**
   - 创建 GitHub repo
   - 初始化 Python 后端（FastAPI + uv）
   - Docker Compose 配置（Postgres）
   - GitHub Actions CI（lint + test）
   - README 草稿

2. **Day 3-4：多账号凭证管理**
   - 实现 RAM Role + STS Token 换证
   - 配置文件 schema 设计（YAML/TOML）
   - 单元测试覆盖正常路径 + 凭证过期

3. **Day 5-7：告警拉取 worker**
   - CloudMonitor API 集成（基于 aliyun-openapi-python-sdk）
   - 定时任务（Celery / APScheduler）
   - 告警入库 schema 设计

4. **Day 8-10：AI 分析 MVP**
   - Claude API 集成
   - 结构化 prompt 模板（根因 + 建议 + 操作清单）
   - CLI 输出格式化

5. **Day 11-14：打磨 + 发布**
   - 完整 README + Quickstart
   - 录一个 5 分钟 demo 视频
   - 发布到 GitHub + 朋友圈 / 技术社区

## What I noticed about how you think

观察到的几个信号（不是评判）：

- 你选了"AI 深度参与"而不是"极致可视化 Dashboard"——2025 年 GitHub 上 AI-Agent 范式最火，你这个判断对 OSS 传播力很关键
- 你选了"开源/学习"——这是务实的入口，意味着你可以用 6-12 个月慢慢打磨；不要被"完美主义"困住
- 你对所有 5 个前提都没反驳——这可能意味着有些前提你没细想；后续真做起来可能会发现 P2 或 P4 没那么强，记得回来 push back
- 你选了"理想架构"而不是"先 2 周验证再扩展"——这表明你愿意承担更大的项目；我的建议是"理想架构但先 2 周 MVP"，两者不冲突

---

**Design lineage**: First design on this branch (`main`).

**Reviewer concerns**: None yet (pending Spec Review Loop).

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | issues_found | 14 issues, 0 critical gaps (all addressed with B option) |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **CROSS-MODEL:** Codex flagged 4 strategic gaps (P1/P2 unvalidated, AI diagnosis undefined, MVP 2-week unrealistic, no kill criterion). All addressed via D12/D13/D14 (B recommendations: MCP validation checkpoint, AI quality bar in M1 W1, M1 redefined as 4 weeks).
- **VERDICT:** ENG CLEARED with adjustments — ready to implement with T1–T14 + T15–T18 deferred TODOS.

**UNRESOLVED DECISIONS:**
- T18 (kill criterion + data retention/residency policy) — must be defined before M2 ships; placeholder added to TODOS but no concrete metrics yet
- T12 (MCP validation checkpoint) — 1-week investment after M1 completion; deferred decision on MCP-vs-Dashboard viability